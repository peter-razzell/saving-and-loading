[gd_scene load_steps=8 format=3 uid="uid://c16wm0u1b377t"]

[ext_resource type="Script" uid="uid://cgpap1urh03x7" path="res://src/Compositor/post_process_shader.gd" id="1_f2bpq"]
[ext_resource type="Texture2D" uid="uid://cforritefvrmr" path="res://Assets/Shader/green_gradient.tres" id="2_qc1ul"]

[sub_resource type="CompositorEffect" id="CompositorEffect_qc1ul"]
resource_local_to_scene = false
resource_name = ""
enabled = true
effect_callback_type = 4
needs_motion_vectors = false
needs_normal_roughness = false
script = ExtResource("1_f2bpq")
shader_code = "shader_type spatial;
render_mode unshaded;

uniform sampler2D screen_texture : hint_screen_texture;

uniform sampler2D u_dither_tex : repeat_enable, filter_nearest;
uniform sampler2D u_color_tex : filter_nearest;

uniform int u_bit_depth = 4;
uniform float u_contrast = 1.0;
uniform float u_offset = 0.0;
uniform int u_dither_size = 1;

void fragment()
{
    // Pixelated sampling
    vec2 screen_size = vec2(textureSize(screen_texture, 0)) / float(u_dither_size);
    vec2 screen_sample_uv = floor(SCREEN_UV * screen_size) / screen_size;
    vec3 screen_col = texture(screen_texture, screen_sample_uv).rgb;

    // Luminosity
    float lum = dot(screen_col, vec3(0.299, 0.587, 0.114));

    // Contrast + offset
    lum = (lum - 0.5 + u_offset) * u_contrast + 0.5;
    lum = clamp(lum, 0.0, 1.0);

    // Bit depth reduction
    float bits = float(u_bit_depth);
    lum = floor(lum * bits) / bits;

    // Palette mapping
    ivec2 col_size = textureSize(u_color_tex, 0);
    col_size /= col_size.y;

    float col_x = float(col_size.x) - 1.0;
    float col_texel_size = 1.0 / col_x;

    lum = max(lum - 0.00001, 0.0);

    float lum_lower = floor(lum * col_x) * col_texel_size;
    float lum_upper = (floor(lum * col_x) + 1.0) * col_texel_size;
    float lum_scaled = lum * col_x - floor(lum * col_x);

    // Dither
    ivec2 noise_size = textureSize(u_dither_tex, 0);
    vec2 inv_noise_size = 1.0 / vec2(noise_size);

    vec2 noise_uv = SCREEN_UV * inv_noise_size * screen_size;
    float threshold = texture(u_dither_tex, noise_uv).r;
    threshold = threshold * 0.99 + 0.005;

    float ramp_val = lum_scaled < threshold ? 0.0 : 1.0;
    float col_sample = mix(lum_lower, lum_upper, ramp_val);

    vec3 final_col = texture(u_color_tex, vec2(col_sample, 0.5)).rgb;

    ALBEDO = final_col;
    ALPHA = 1.0;
}"
metadata/_custom_type_script = "uid://cgpap1urh03x7"

[sub_resource type="Compositor" id="Compositor_pol14"]
compositor_effects = Array[CompositorEffect]([SubResource("CompositorEffect_qc1ul")])

[sub_resource type="Environment" id="Environment_d4t0q"]

[sub_resource type="CompositorEffect" id="CompositorEffect_f2bpq"]
resource_local_to_scene = false
resource_name = ""
enabled = true
effect_callback_type = 4
needs_motion_vectors = false
needs_normal_roughness = false
script = ExtResource("1_f2bpq")
palette_texture = ExtResource("2_qc1ul")
metadata/_custom_type_script = "uid://cgpap1urh03x7"

[sub_resource type="Compositor" id="Compositor_qc1ul"]
compositor_effects = Array[CompositorEffect]([SubResource("CompositorEffect_f2bpq")])

[node name="Control" type="Control"]
visibility_layer = 2
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2

[node name="baseLayer" type="SubViewportContainer" parent="."]
unique_name_in_owner = true
light_mask = 0
visibility_layer = 2
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
stretch = true

[node name="SubViewport" type="SubViewport" parent="baseLayer"]
handle_input_locally = false
screen_space_aa = 2
use_taa = true
canvas_cull_mask = 4294966274
size = Vector2i(1280, 720)
render_target_update_mode = 4

[node name="Layer1Camera" type="Camera3D" parent="baseLayer/SubViewport"]
cull_mask = 1
compositor = SubResource("Compositor_pol14")
current = true
size = 20.0

[node name="Layer2" type="SubViewportContainer" parent="."]
unique_name_in_owner = true
layout_mode = 0
offset_right = 40.0
offset_bottom = 40.0

[node name="SubViewport" type="SubViewport" parent="Layer2"]
transparent_bg = true
handle_input_locally = false
size = Vector2i(1280, 720)
render_target_update_mode = 4

[node name="Layer2Camera" type="Camera3D" parent="Layer2/SubViewport"]
cull_mask = 2
environment = SubResource("Environment_d4t0q")
compositor = SubResource("Compositor_qc1ul")
current = true

[node name="Layer3" type="SubViewportContainer" parent="."]
unique_name_in_owner = true
layout_mode = 0
offset_right = 40.0
offset_bottom = 40.0

[node name="SubViewport" type="SubViewport" parent="Layer3"]
transparent_bg = true
handle_input_locally = false
size = Vector2i(1280, 720)
render_target_update_mode = 4

[node name="Layer3Camera" type="Camera3D" parent="Layer3/SubViewport"]
cull_mask = 4
environment = SubResource("Environment_d4t0q")
current = true
